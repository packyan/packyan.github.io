# Java 基础

![TOC]

## Java 发展

简单性 可移植性 面向对象 解释性 分布式 高性能 健壮性 多线程 安全性 动态性

体系结构中立

 

 

命名规则：

 

访问修饰符： public 关键字 public 称为访问修饰符(access modifier), 这 些修饰符用于控制程序的其他部分对这段代码的访问级別 

 关键字 class 后面紧跟类名。 Java 中定义类名的规则很宽松。 名字必须以字母开头， 后 面可以跟字母和数字的任意组合。长度基本上没有限制。但是不能使用 Java 保留字(例如， public或class) 作为类名(保留字列表请参看附录A)。 

标准的命名规范为(类名 FirstSample 就遵循了这个规范): 类名是以大写字母开头的名 词。如果名字由多个单词组成， 每个单词的第一个字母都应该大写(这种在一个单词中间使 用大写字母的方式称为胳蛇命名法。 以其自身为例， 应该写成 CamelCase )。 

重要： **类名和文件名需要一致** 

Java 中的所有函數都属于**某个类的方法**(标准 术语将其称为方法， 而不是成员函数) 